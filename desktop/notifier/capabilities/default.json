{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The popup only listens for cards and invokes this app's own three commands. Window moves and show/hide are driven from Rust, so the page needs no window permissions of its own.",
  "windows": ["stack"],
  "permissions": ["core:default"]
}
